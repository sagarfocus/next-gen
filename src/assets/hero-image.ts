// Hero image — modern healthcare facility.
// Imported as a Vite asset (hashed at build time, browser-cached).
// Swap with a different image any time by replacing `hero.png` in this folder.
import heroImg from './hero.png';

export const HERO_IMAGE_URL = heroImg;
export const HERO_IMAGE_ALT =
  'Modern healthcare facility — TheNextGen Healthcare Marketing';
